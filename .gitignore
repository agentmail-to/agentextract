node_modules/
*.log
.DS_Store
dist/
coverage/

# Evals (never commit) — harnesses run against real production data.
# The whole dir stays local: corpus, answer keys, cached engine outputs, and fixtures.
eval/
*.jsonl
threads.jsonl
